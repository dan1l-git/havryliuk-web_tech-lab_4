version: '3.8'
services:
  casdoor:
    image: casbin/casdoor-all-in-one:latest
    ports:
      - "8000:8000"
  nginx:
    image: nginx:alpine
    ports:
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./certs:/etc/nginx/certs
    depends_on:
      - casdoor